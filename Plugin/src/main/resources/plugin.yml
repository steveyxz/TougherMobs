name: TougherMobsCore
version: '${project.version}'
main: me.partlysunny.TougherMobsCore
api-version: 1.16
commands:
  tmobs:
    aliases: [ toughermobs, tmobs ]
    description: TougherMobs base command for ops

permissions:
  tmobs.admin:
    default: op
    description: Admin commands (from /tmobs)